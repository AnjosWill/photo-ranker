# Feature 4.1 — Contest UI Base

> **Objetivo:** Criar a estrutura básica da interface do Contest Mode com layout, navegação e estados vazios.

---

## 🎯 Objetivo

Implementar a aba "Contest" com interface base para confrontos entre fotos, incluindo:
- Layout responsivo para exibição lado a lado (A vs B)
- Navegação e estados
- Preparação para lógica de batalhas (implementada em F4.3)

---

## 📦 Escopo

### In Scope:
- [x] HTML da aba "Contest" (estrutura completa)
- [x] CSS para layout de confronto (desktop e mobile)
- [x] Estados vazios ("Nenhuma foto com 5 estrelas para contest")
- [x] Botão "Iniciar Contest" (placeholder, será funcional em F4.3)
- [x] Navegação básica (atalhos de teclado)
- [x] Responsividade (vertical em mobile, horizontal em desktop)

### Out of Scope:
- ❌ Lógica de batalhas (F4.3)
- ❌ Cálculo Elo (F4.2)
- ❌ Tela de resultados (F4.4)
- ❌ Múltiplos projetos (F4.5, F4.6)

---

## 🧩 Requisitos Funcionais (RF)

### RF1 — Layout de Confronto
- **RF1.1**: Aba "Contest" exibe 2 placeholders de fotos lado a lado (desktop)
- **RF1.2**: Em mobile (< 600px), fotos ficam verticais (uma acima da outra)
- **RF1.3**: Área de 800×600px (max) para cada foto em desktop
- **RF1.4**: Fotos com `object-fit: contain` (mantém proporção)

### RF2 — Estados
- **RF2.1**: Estado inicial: "Nenhum contest ativo"
- **RF2.2**: Estado vazio: "Você precisa de pelo menos 2 fotos com ⭐5 para iniciar"
- **RF2.3**: Botão "Iniciar Contest" desabilitado se < 2 fotos com rating 5
- **RF2.4**: Contador: "X fotos qualificadas" (rating = 5)

### RF3 — Navegação
- **RF3.1**: Esc fecha contest e volta para aba Upload
- **RF3.2**: Tab navega entre elementos (botões, fotos)
- **RF3.3**: Foco visível em todos os elementos interativos

---

## 🎨 Requisitos Não Funcionais (RNF)

### RNF1 — UX
- **RNF1.1**: Layout centralizado e simétrico
- **RNF1.2**: Transições suaves ao trocar de aba
- **RNF1.3**: Feedback visual ao passar mouse (hover)
- **RNF1.4**: Instruções claras ("Escolha a melhor foto")

### RNF2 — Responsividade
- **RNF2.1**: Desktop: fotos lado a lado
- **RNF2.2**: Mobile: fotos verticais (uma acima da outra)
- **RNF2.3**: Botões com min 44×44px (WCAG)

### RNF3 — Acessibilidade
- **RNF3.1**: ARIA labels em todos os elementos
- **RNF3.2**: Navegação por teclado 100% funcional
- **RNF3.3**: Contraste WCAG AA

---

## 🧱 Arquitetura

### Arquivos Novos:
- Nenhum (apenas HTML/CSS)

### Arquivos Modificados:
- `public/index.html` - Estrutura da aba "Contest"
- `public/styles/components.css` - Estilos do contest
- `public/scripts/app.js` - Função `renderContestView()` (placeholder)

---

## 📐 Estrutura HTML

```html
<section data-route="contest" class="route">
  <h2 class="sr-only">Contest</h2>
  
  <!-- Estado: Nenhum contest ativo -->
  <div id="contestView" class="contest-view">
    <div class="contest-empty">
      <div class="contest-empty-icon">🏆</div>
      <h3>Contest Mode</h3>
      <p>Compare fotos lado a lado e escolha a melhor!</p>
      
      <div class="contest-stats">
        <span class="contest-stat">
          <strong id="qualifiedCount">0</strong> fotos qualificadas (⭐5)
        </span>
      </div>
      
      <button id="startContest" class="btn" disabled>
        Iniciar Contest
      </button>
      
      <p class="muted">Você precisa de pelo menos 2 fotos com ⭐5</p>
    </div>
  </div>
</section>
```

---

## 🎨 Design CSS

### Layout de Confronto (será usado em F4.3):
```css
.contest-battle {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--gap-4);
  max-width: 1600px;
  margin: 0 auto;
}

.battle-photo {
  position: relative;
  background: #000;
  border-radius: 12px;
  overflow: hidden;
  cursor: pointer;
  transition: all 0.2s ease;
}

.battle-photo:hover {
  transform: scale(1.02);
  box-shadow: 0 8px 32px rgba(106, 163, 255, 0.4);
}

@media (max-width: 600px) {
  .contest-battle {
    grid-template-columns: 1fr; /* Vertical */
  }
}
```

---

## ✅ Definition of Done

- [ ] Aba "Contest" renderiza corretamente
- [ ] Estado vazio mostra mensagem apropriada
- [ ] Botão "Iniciar Contest" desabilitado quando < 2 fotos ⭐5
- [ ] Contador de fotos qualificadas funcional
- [ ] Responsivo (desktop e mobile)
- [ ] Navegação por teclado (Esc, Tab)
- [ ] Sem erros de linter
- [ ] Documentação atualizada (README, CHANGELOG)

---

## 🔗 Dependências

- **Depende de:** Nada (independente)
- **Bloqueia:** F4.3 (Battle Interface)

---

## 📝 Notas Técnicas

### Função Placeholder:

```javascript
// app.js
function renderContestView() {
  const container = $('#contestView');
  if (!container) return;
  
  // Contar fotos qualificadas (rating = 5)
  const allPhotos = await getAllPhotos();
  const qualifiedPhotos = allPhotos.filter(p => !p._isSplit && p.rating === 5);
  
  $('#qualifiedCount').textContent = qualifiedPhotos.length;
  
  const startBtn = $('#startContest');
  if (startBtn) {
    startBtn.disabled = qualifiedPhotos.length < 2;
  }
}

// Observar mudança de hash
window.addEventListener('hashchange', () => {
  if (location.hash === '#/contest') {
    renderContestView();
  }
});
```

---

**Estimativa:** 2-3 horas  
**Prioridade:** Alta (base para F4.3)  
**Complexidade:** Baixa

